'''a module is a file consisting of Python code.
A module can define functions, classes and variables.'''

# Built in module : math
from math import pow      # We have imported pow() from math module / package
print(pow(2,3))